#!/usr/bin/env bash

# Start PATH from here (default path in MacOS)
export PATH="/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin"

# Setting PATH for personal scripts
export PATH="${PATH}:${HOME}/projects/bin"
