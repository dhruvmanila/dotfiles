# AGENTS.md

* The `dotbot` file is auto-generated using `bashly` command. The `Makefile`
  has the target to perform this task. Run `make` directly as this is the
  default task. You can run `make --always-make` to always run the default
  task.
