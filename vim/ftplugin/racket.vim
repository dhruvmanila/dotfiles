set tabstop=2
set expandtab
set shiftwidth=2
set softtabstop=2

" Enable auto begin new comment line when continuing from an old comment line
" using double colons
setl comments+=:;;

" This does not seem to work when kept in this file but it works when it is in
" the vimrc file
" let g:NERDCustomDelimiters = { 'racket': { 'left': ';;' , 'leftAlt': ';' } }
