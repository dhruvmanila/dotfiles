name: dotbot
help: Personal tool for upgrading packages
version: 1.0.0

commands:
  - name: upgrade
    help: upgrade packages
    # catch_all:
    #   label: additional packages
    #   help: addtional packages to upgrade from the 'Allowed' list mentioned above

    args:
      - name: package
        help: package to upgrade
        default: all
        allowed:
          - all
          - brew
          - cargo
          - neovim
          - nnn
          - npm
          - port
          - plugins
          - python

    flags:
      - long: --ref
        short: -r
        arg: ref
        help: |
          Upgrade to a specific ref (commit/branch/tag). This flag is applicable only when 'PACKAGE' is:
            - neovim
